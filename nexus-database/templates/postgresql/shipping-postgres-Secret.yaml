{{- if and .Values.postgresql.enabled .Values.postgresql.shipping.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.postgresql.shipping.name }}-secret
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "nexus-database.postgresql.labels" . | nindent 4 }}
    app: {{ .Values.postgresql.shipping.name }}
    service: shipping-service
type: Opaque
data:
  username: {{ .Values.postgresql.shipping.auth.username | b64enc }}
  password: {{ .Values.postgresql.shipping.auth.password | b64enc }}
  database: {{ .Values.postgresql.shipping.database.name | b64enc }}
  host: {{ printf "%s-service" .Values.postgresql.shipping.name | b64enc }}
  port: {{ .Values.postgresql.shipping.service.port | toString | b64enc }}
{{- end }}