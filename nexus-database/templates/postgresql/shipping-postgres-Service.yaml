{{- if and .Values.postgresql.enabled .Values.postgresql.shipping.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.postgresql.shipping.name }}-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "nexus-database.postgresql.labels" . | nindent 4 }}
    app: {{ .Values.postgresql.shipping.name }}
    service: shipping-service
spec:
  clusterIP: None
  selector:
    app: {{ .Values.postgresql.shipping.name }}
  ports:
    - name: postgres
      port: {{ .Values.postgresql.shipping.service.port }}
      targetPort: {{ .Values.postgresql.shipping.service.port }}
      protocol: TCP
{{- end }}